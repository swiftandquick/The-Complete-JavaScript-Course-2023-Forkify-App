// Create and export the variable API_URL, which contains the URL of the Forkify API.  
export const API_URL = 'https://forkify-api.herokuapp.com/api/v2/recipes/';

// Create and export TIMEOUT_SEC, which has the value of 10.  
export const TIMEOUT_SEC = 10;

// Create and export RES_PER_PAGE, which represent the number of elements in each page for display, which is 10.  
export const RES_PER_PAGE = 10;

// API key for submitting recipes.  
export const KEY = '88ede4e8-0f91-4d8d-a6d8-8c3df080470d';

// Modal closes in 2.5 seconds.  
export const MODAL_CLOSE_SEC = 2.5;